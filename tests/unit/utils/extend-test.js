import extend from 'ember-es-adapter/utils/extend';
import { module, test } from 'qunit';

module('Unit | Utility | extend');

// Replace this with your real tests.
test('Can load utililty', function(assert) {
  let util = extend;
  assert.ok(util);
});
