import EsMapper from 'dummy/utils/es-mapper';
import { module, test } from 'qunit';

module('Unit | Utility | es mapper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = new EsMapper();

  assert.ok(result.getTrue());
});
